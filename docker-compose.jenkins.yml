# Fichier: docker-compose.jenkins.yml (Mis à jour)
version: '3.8'

services:
  # Service 1: Laravel
  user_service:
    build:
      context: ./services/user-service
      dockerfile: Dockerfile
    container_name: user_service_test_app
    networks:
      - events_network

  # Service 2: Symfony (NOUVEAU)
  event_service:
    build:
      context: ./services/event-service
      dockerfile: Dockerfile
    container_name: event_service_test_app
    networks:
      - events_network

networks:
  events_network:
    external: true
    name: projet-gestion-evenements_events_network